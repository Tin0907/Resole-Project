# STICKY NAVBAR - HÝ?NG D?N C?P NH?T

## Tính Nãng

Menu header (navbar) bây gi? s? **dính (sticky) ? trên cùng TOÀN B? TRANG WEB** khi b?n cu?n xu?ng, không ch? trong ph?n hero.

## Thay Ð?i Quan Tr?ng

### 1. Tách Navbar Ra Kh?i Hero Section

**Trý?c ðây:** Navbar n?m B?N TRONG ph?n `.rs-hero` ? Khi cu?n qua hero section th? navbar bi?n m?t

**Bây gi?:** Navbar ðý?c tách ra ngoài ? Navbar sticky trên TOÀN B? TRANG

### 2. C?u Trúc HTML M?i (Views/Shared/_WebLayout.cshtml)

```html
<body>
    <!-- Navbar - Ngoài cùng, sticky toàn trang -->
    <nav class="navbar rs-sticky-nav">
        <!-- Navbar content -->
    </nav>

    <!-- Hero Section - Ch? hi?n th? trên trang UserHome/Index -->
    @if (trang home) {
        <header class="rs-hero">
            <!-- Hero content -->
        </header>
    }

    <!-- Main Content -->
    <main>
        @RenderBody()
    </main>
</body>
```

### 3. CSS M?i

#### Sticky Navbar
```css
.rs-sticky-nav {
  position: sticky;
  top: 0;
  z-index: 1000;
  background: linear-gradient(...);
  backdrop-filter: blur(10px);
}
```

**L?i ích:**
- ? Sticky trên TOÀN B? trang web
- ? Không b? gi?i h?n b?i hero section
- ? Luôn hi?n th? khi cu?n

#### Hero Section Riêng
```css
.rs-hero {
  min-height: 400px;
  /* Không c?n ch?a navbar */
}
```

### 4. Logic Hi?n Th? Hero

Hero section CH? hi?n th? trên:
- Trang UserHome (`/Home/UserHome`)
- Trang Index (`/Home/Index`)

Các trang khác (Shop, About, Cart, etc.) ? KHÔNG có hero section ? CH? có sticky navbar

## K?t Qu?

### ? Trên Trang UserHome
- Navbar sticky ? trên cùng
- Hero section v?i background image phía dý?i
- Khi cu?n xu?ng ? Navbar v?n dính ? trên
- Cu?n qua hero ? Navbar v?n c?n ðó!

### ? Trên Các Trang Khác (Shop, Cart, etc.)
- Navbar sticky ? trên cùng
- KHÔNG có hero section
- Cu?n xu?ng ? Navbar v?n dính

### ? Scroll Effect
- Cu?n > 50px ? Navbar t?i hõn + bóng ð? m?nh hõn
- Cu?n v? top ? Navbar sáng l?i

## Test Ngay

1. **Ch?y l?i ?ng d?ng** (F5 ho?c Ctrl+F5)
2. M? trang **UserHome**
3. **Cu?n xu?ng qua hero section** ? Menu v?n dính ? trên! ?
4. **Cu?n ti?p xu?ng** ? Menu v?n ? ðó! ?
5. Chuy?n sang trang **Shop** ? Menu v?n sticky! ?
6. Cu?n trang Shop ? Menu v?n dính! ?

## So Sánh Trý?c/Sau

### ? Trý?c
```
???????????????????
?   Navbar        ? ? Sticky
???????????????????
?   Hero Content  ?
?                 ?
???????????????????
? Cu?n qua ðây...
???????????????????
?   Main Content  ? ? Navbar BI?N M?T!
?                 ?
???????????????????
```

### ? Sau
```
???????????????????
?   Navbar        ? ? Sticky TOÀN TRANG
???????????????????
?   Hero Content  ?
?  (ch? trang home)?
???????????????????
? Cu?n...
???????????????????
?   Navbar        ? ? V?N C?N ÐÂY!
???????????????????
?   Main Content  ?
?                 ?
???????????????????
```

## Files Ð? S?a

1. ? `Views/Shared/_WebLayout.cshtml` - Tách navbar ra ngoài hero
2. ? `wwwroot/css/site.css` - CSS m?i cho `.rs-sticky-nav`
3. ? JavaScript - C?p nh?t t? `.rs-hero-nav` ? `.rs-sticky-nav`

## Tùy Ch?nh

### Thay ð?i chi?u cao navbar
```css
.rs-sticky-nav {
  padding: 0.75rem 0; /* Ð?i thành 1rem, 0.5rem, etc. */
}
```

### Thay ð?i màu n?n
```css
.rs-sticky-nav {
  background: rgba(0, 0, 0, 0.95); /* Màu ðen ð?m */
}
```

### T?t hero section trên trang home
Trong `_WebLayout.cshtml`, xóa ho?c comment ph?n:
```razor
@if (ViewContext.RouteData.Values["action"]?.ToString() == "UserHome" || ...)
{
    <header class="rs-hero">...</header>
}
```

## Troubleshooting

### Navbar v?n bi?n m?t khi cu?n?
- Clear browser cache (Ctrl+Shift+R)
- Ki?m tra navbar ð? n?m NGOÀI `<header class="rs-hero">` chýa
- Ki?m tra class ð? ð?i t? `.rs-hero-nav` ? `.rs-sticky-nav` chýa

### Hero section hi?n th? trên t?t c? trang?
- Ki?m tra ði?u ki?n `@if` trong `_WebLayout.cshtml`
- Ð?m b?o ch? check action "UserHome" ho?c "Index"

### Dropdown b? che?
- Ki?m tra `z-index` c?a `.dropdown-menu` (ph?i > 1000)

---

**HOÀN THÀNH!** ??

Navbar c?a b?n gi? **STICKY TOÀN TRANG**, không bao gi? bi?n m?t!
